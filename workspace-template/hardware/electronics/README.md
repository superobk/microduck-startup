# Electronics

保存 HAT、IMU-to-Dynamixel、供电、UART/I2C/I2S/CSI、主控平替和逻辑分析资料。

建议结构：

```text
interfaces/
power/
imu-to-dxl/
hat/
controller-alternatives/
bringup/
```

二进制采样和大附件放 `artifacts/`，文档只引用 hash 与相对路径。
