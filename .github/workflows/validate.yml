name: validate

on:
  push:
  pull_request:

jobs:
  startup-static-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate shell and Python syntax
        run: make validate
